    <!--
       ========================================================================
         Run Tomcat 3.2 tests
       ========================================================================
    -->
    <target name="test.tomcat.32" depends="prepare.test.tomcat.32"
        if="tomcat.home.32" description="Run tests on Tomcat 3.2">

        <!-- Start the servlet engine, wait for it to be started, run the
             unit tests, stop the servlet engine, wait for it to be stopped.
             The servlet engine is stopped if the tests fail for any reason -->

        <runservertests testURL="http://localhost:${test.port}/test"
            startTarget="start.tomcat.32"
            stopTarget="stop.tomcat.32"
            testTarget="test"/>

    </target>

    <!--
       ========================================================================
         Start Tomcat 3.2
       ========================================================================
    -->
    <target name="start.tomcat.32">

        <java classname="org.apache.tomcat.startup.Tomcat" fork="yes">
            <arg value="-config"/>
            <arg value="${target.tomcat32.dir}/conf/server.xml"/>
            <classpath>
                <pathelement location="${java.home}/../lib/tools.jar"/>
                <fileset dir="${tomcat.home.32}/lib">
                    <include name="*.jar"/>
                </fileset>
            </classpath>
        </java>

    </target>

    <!--
       ========================================================================
         Stop Tomcat 3.2
       ========================================================================
    -->
    <target name="stop.tomcat.32">

        <java classname="org.apache.tomcat.startup.Tomcat" fork="yes">
            <jvmarg value="-Dtomcat.home=${tomcat.home.32}"/>
            <arg value="-stop"/>
            <arg value="-config"/>
            <arg value="${target.tomcat32.dir}/conf/server.xml"/>
            <classpath>
                <pathelement location="${java.home}/../lib/tools.jar"/>
                <fileset dir="${tomcat.home.32}/lib">
                    <include name="*.jar"/>
                </fileset>
            </classpath>
        </java>

    </target>

    <!--
       ========================================================================
         Display a warning message if the needed servlet engine home property
         is not set
       ========================================================================
    -->
    <target name="check.test.tomcat.32" depends="testwar"
        unless="tomcat.home.32">

        <echo message=""/>
        <echo message="******************************************************"/>
        <echo message="WARNING : The 'tomcat.home.32' property has not been "/>
        <echo message="set. No test will be run on that servlet engine."/>
        <echo message="******************************************************"/>
        <echo message=""/>

    </target>

    <!--
       ========================================================================
         Prepare directories and variables for running the tests
       ========================================================================
    -->
    <target name="prepare.test.tomcat.32"
        depends="check.test.tomcat.32,prepare.test" if="tomcat.home.32">

        <echo message="tomcat.home.32 = ${tomcat.home.32}"/>

        <property name="target.tomcat32.dir"
            value="${target.test.dir}/tomcat32"/>
        <property name="conf.tomcat32.dir" value="${conf.test.dir}/tomcat32"/>
        <filter token="target.tomcat32.dir" value="${target.tomcat32.dir}"/>

        <!-- Create work and conf directories and copy configuration files -->
        <mkdir dir="${target.tomcat32.dir}/conf"/>
        <mkdir dir="${target.tomcat32.dir}/work"/>
        <mkdir dir="${target.tomcat32.dir}/webapps"/>

        <!-- Delete some config file so that they will be copied every time -->
        <delete file="${target.tomcat32.dir}/conf/testserver.xml"/>

        <!-- Delete the expanded war so that the new one is picked up -->
        <delete dir="${target.tomcat32.dir}/webapps"/>

        <!-- Copy the default tomcat web.xml to our test conf/ directory.
             This is needed otherwise tomcat does not start -->
        <copy file="${tomcat.home.32}/conf/web.xml"
            tofile="${target.tomcat32.dir}/conf/web.xml"/>

        <copy file="${conf.tomcat32.dir}/server.xml"
            tofile="${target.tomcat32.dir}/conf/server.xml" filtering="on"/>

        <!-- Copy the war file -->
        <copy file="${target.test.dir}/test.war"
            tofile="${target.tomcat32.dir}/webapps/test.war"/>

    </target>
