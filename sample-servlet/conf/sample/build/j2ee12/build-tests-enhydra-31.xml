    <!--
       ========================================================================
         Run Enhydra 3.1 tests
       ========================================================================
    -->
    <target name="test.enhydra.31" depends="prepare.test.enhydra.31"
        if="enhydra.home.31" description="Run tests on Enhydra 3.1">

        <!-- Start the servlet engine, wait for it to be started, run the
             unit tests, stop the servlet engine, wait for it to be stopped.
             The servlet engine is stopped if the tests fail for any reason -->

        <runservertests testURL="http://localhost:${test.port}/test"
            startTarget="start.enhydra.31"
            stopTarget="stop.enhydra.31"
            testTarget="test"/>

    </target>

    <!--
       ========================================================================
         Start Enhydra 3.1
       ========================================================================
    -->
    <target name="start.enhydra.31">

        <java classname="org.apache.cactus.ant.EnhydraRun" fork="yes">
            <arg value="-start"/>
            <arg value="${target.enhydra31.dir}/conf/multiserver.conf"/>
            <classpath>
                <fileset dir="${enhydra.home.31}/lib">
                    <include name="enhydra.jar"/>
                    <include name="admin.jar"/>
                </fileset>
                <pathelement location="${cactus.ant.jar}"/>
                <pathelement location="${java.home}/../lib/tools.jar"/>
            </classpath>
        </java>

    </target>

    <!--
       ========================================================================
         Stop Enhydra 3.1
       ========================================================================
    -->
    <target name="stop.enhydra.31">

        <java classname="org.apache.cactus.ant.EnhydraRun" fork="yes">
            <arg value="-stop"/>
            <classpath>
                <fileset dir="${enhydra.home.31}/lib">
                    <include name="enhydra.jar"/>
                    <include name="admin.jar"/>
                </fileset>
                <pathelement location="${cactus.ant.jar}"/>
                <pathelement location="${java.home}/../lib/tools.jar"/>
            </classpath>
        </java>

    </target>

    <!--
       ========================================================================
         Display a warning message if the needed servlet engine home property
         is not set
       ========================================================================
    -->
    <target name="check.test.enhydra.31" depends="testwar"
        unless="enhydra.home.31">

        <echo message=""/>
        <echo message="******************************************************"/>
        <echo message="WARNING : The 'enhydra.home.31' property has not been"/>
        <echo message="set. No test will be run on that servlet engine."/>
        <echo message="******************************************************"/>
        <echo message=""/>

    </target>

    <!--
       ========================================================================
         Prepare directories and variables for running the tests
       ========================================================================
    -->
    <target name="prepare.test.enhydra.31"
        depends="check.test.enhydra.31,prepare.test" if="enhydra.home.31">

        <echo message="enhydra.home.31 = ${enhydra.home.31}"/>

        <property name="target.enhydra31.dir"
            value="${target.test.dir}/enhydra31"/>
        <property name="conf.enhydra31.dir" value="${conf.test.dir}/enhydra31"/>

        <pathconvert targetos="unix" property="full.target.enhydra31.dir" >
            <path>
                <pathelement location="${basedir}/${target.enhydra31.dir}"/>
            </path>
        </pathconvert>

        <echo message="full.target.enhydra31.dir = ${full.target.enhydra31.dir}"/>

        <filter token="target.enhydra31.dir" value="${target.enhydra31.dir}"/>
        <filter token="full.target.enhydra31.dir"
            value="${full.target.enhydra31.dir}"/>

        <!-- Create work and conf directories and copy configuration files -->
        <mkdir dir="${target.enhydra31.dir}/conf"/>
        <mkdir dir="${target.enhydra31.dir}/work"/>
        <mkdir dir="${target.enhydra31.dir}/webapps/test"/>

        <!-- Copy the configuration file -->
        <copy file="${conf.enhydra31.dir}/multiserver.conf"
            tofile="${target.enhydra31.dir}/conf/multiserver.conf"
            filtering="on"/>

        <!-- This is needed here because Enhydra 3.1 does not support
             automatic war deployment  -->
        <unwar src="${target.test.dir}/test.war"
            dest="${target.enhydra31.dir}/webapps/test"/>

    </target>
