<!-- build script to create JUnit Plugin -->
<project name="org.apache.cactus.eclipse" default="export" basedir="..">
	<target name="init">
		<tstamp/>
		<property name="destdir" value="../../plugin-export" />
		<property name="plugin"  value="org.apache.cactus.eclipse" />
		<property name="version"  value="_0.1.0" />
		<property name="dest"  value="${destdir}/${plugin}${version}" />
	</target>

	<target name="build" depends="init">
    	<eclipse.incrementalBuild project="${plugin}" kind="incr"/>
	</target>

	<target name="export" depends="build">
		<mkdir dir="${destdir}" />
		<delete dir="${dest}" />
		<mkdir dir="${dest}" />
		
		<jar 
			jarfile="${dest}/cactussupport.jar"
			basedir="bin"
		/>
				
		<jar 		
			jarfile="${dest}/cactussupportsrc.jar"
			basedir="src/java"
		/>
		
		<copy file="plugin.xml" todir="${dest}"/>
		<copy file="plugin.properties" todir="${dest}"/>
		<copy todir="${dest}/icons">
			<fileset dir="icons" />
		</copy>
		<copy todir="${dest}/ant">
			<fileset dir="ant" />
		</copy>
	</target>
</project>
